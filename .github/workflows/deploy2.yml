name: Deploy to shinyapps.io

on:
  push:
    branches: [ main, master ]

jobs:
  shiny-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: false

      - name: Install system dependencies (Required for spatial packages)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgdal-dev libgeos-dev libproj-dev libudunits2-dev

      - name: Install R Packages and Deploy
        env:
          SHINYAPPS_ACCOUNT: ${{ secrets.SHINYAPPS_ACCOUNT }}
          SHINYAPPS_TOKEN: ${{ secrets.SHINYAPPS_TOKEN }}
          SHINYAPPS_SECRET: ${{ secrets.SHINYAPPS_SECRET }}
        run: |
          Rscript -e "
            options(repos = c(CRAN = 'https://cran.rstudio.com/'))
            
            # Use minimal memory footprint optimization flags for compilation
            install.packages(
              c('rsconnect', 'shiny', 'sf', 'leafem', 'leaflet', 'mapview',
                'janitor', 'DBI', 'RSQLite'), 
              clean = TRUE, 
              INSTALL_opts = c('--no-docs', '--no-html', '--no-multiarch')
            )
            
            # Pin terra with the same optimization flags
            install.packages(
              'https://cran.r-project.org/src/contrib/Archive/terra/terra_1.8-10.tar.gz', 
              repos = NULL, 
              type = 'source',
              clean = TRUE,
              INSTALL_opts = c('--no-docs', '--no-html', '--no-multiarch')
            )
            
            rsconnect::setAccountInfo(
              name = Sys.getenv('SHINYAPPS_ACCOUNT'),
              token = Sys.getenv('SHINYAPPS_TOKEN'),
              secret = Sys.getenv('SHINYAPPS_SECRET')
            )
            
            # Regenerate manifest so that newly added packages (DBI, RSQLite) are
            # captured and installed on the shinyapps.io server during deployment
            rsconnect::writeManifest(appDir = '.')
            
            rsconnect::deployApp(
              appDir = '.',
              appName = 'StreetsDataSF',
              appMode = 'shiny',
              forceUpdate = TRUE
            )
          "
